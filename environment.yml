name: ultraedge-aipc-studio
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - nodejs=20
  - pip:
      # Keep one dependency source of truth. Run conda env create/update from
      # the repository root so this relative path resolves correctly.
      - -r backend/requirements.txt
